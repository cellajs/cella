/**
 * Initializes all mock example registrations.
 * Import this file to ensure all mock examples are registered
 * before calling getExampleForSchema.
 *
 * To add a new entity's examples:
 * 1. Add registerExample() call in that entity's mock file
 * 2. Add the import for that mock file here
 */

import './mock-attachment';
import './mock-common';
import './mock-me';
import './mock-membership';
import './mock-organization';
import './mock-page';
import './mock-user';
