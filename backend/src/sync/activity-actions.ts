/**
 * Re-export from shared - the canonical definition now lives there.
 * Kept for backward compatibility with existing backend imports.
 */
export { type ActivityAction, activityActions } from 'shared';
