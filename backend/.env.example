# DON'T EVER EXPOSE - NOT EVEN TEMPORARILY - PRODUCTION KEYS OR SECRETS IN .ENV FILE. 
# FOR PRODUCTION, SET ENVIRONMENT VARIABLES MANUALLY IN THE HOSTING SERVICE.

#*******************************************************************************
# REQUIRED ENVIRONMENT VARIABLES
#*******************************************************************************

# Node environment
NODE_ENV=development

# Database connections
# DATABASE_URL uses runtime_role (subject to RLS)
# DATABASE_ADMIN_URL uses superuser for migrations, system admin access, and auto-creating roles
# DATABASE_CDC_URL uses cdc_role (INSERT on activities, counters only - append-only)
# Roles (runtime_role, cdc_role, admin_role) are auto-created on startup using passwords from these URLs
DATABASE_URL=postgres://runtime_role:dev_password@0.0.0.0:5432/postgres
DATABASE_ADMIN_URL=postgres://postgres:postgres@0.0.0.0:5432/postgres
DATABASE_CDC_URL=postgres://cdc_role:dev_password@0.0.0.0:5432/postgres

# Optional: separate password for admin_role (defaults to runtime_role password if not set)
# DATABASE_ADMIN_ROLE_PASSWORD=

# Secret for email unsubscribe token
UNSUBSCRIBE_SECRET=some_secret_token

# Secret for argon2id password
ARGON_SECRET=argon_two_secret

# Secret for cookies
COOKIE_SECRET=cookie_secret

# Remote system access IP (for multiple IPs, separate by comma)
REMOTE_SYSTEM_ACCESS_IP=*

# CDC Worker authentication (optional in quick mode)
CDC_INTERNAL_SECRET=some_internal_secret_for_cdc_worker

# Admin email for initial admin user seeding
ADMIN_EMAIL=admin-test@cellajs.com

#*******************************************************************************
# OPTIONAL ENVIRONMENT VARIABLES
#*******************************************************************************

# Debug mode 
DEBUG=false

# Backend port
PORT=4000

# Public URL of the development tunnel
TUNNEL_URL=
TUNNEL_AUTH_TOKEN=

# Brevo
BREVO_API_KEY=
SEND_ALL_TO_EMAIL=

# OAuth strategies
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MICROSOFT_TENANT_ID=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=

# Upload credentials
TRANSLOADIT_KEY=your_transloadit_key
TRANSLOADIT_SECRET=your_transloadit_secret
S3_ACCESS_KEY_ID=
S3_ACCESS_KEY_SECRET=

# Pino
PINO_LOG_LEVEL=info

# Element
ELEMENT_ROOM_ID=
ELEMENT_BOT_ACCESS_TOKEN=