// This file is auto-generated by openapi-parser plugin
import type { GenTagSummary } from '~/modules/docs/types';
/**
 * Tag names as const tuple for type-safe enum validation
 */
export const tagNames = [
  'me',
  'users',
  'memberships',
  'organizations',
  'requests',
  'entities',
  'system',
  'auth',
  'attachments',
  'metrics',
  'pages',
] as const;

export type TagName = (typeof tagNames)[number];

export const tags: GenTagSummary[] = [
  {
    name: 'me',
    description:
      'Endpoints related to the *current user*, meaning the user associated with the active session making the request.\n      These routes are distinct from general `users` endpoints: while `users` may operate on any user in the system, `me` endpoints are scoped exclusively to the *current user* and follow a different authorization flow.',
    count: 10,
  },
  {
    name: 'users',
    description:
      'Endpoints for managing *users* at the system level.\n      Unlike context entities (such as `organizations`), a `user` is a "global" entity and not scoped to a specific context.\n      These endpoints are intended for administrative operations on any user in the system.',
    count: 4,
  },
  {
    name: 'memberships',
    description:
      'Endpoints for managing *memberships*, which represent one-to-one relationships between a `user` and a contextual `entity` (e.g., an `organization`).  \n      Each membership includes role information and status flags such as `archived` or `muted`.  \n      Memberships can also reference parent entities, enabling hierarchical context.',
    count: 6,
  },
  {
    name: 'organizations',
    description:
      'Endpoints for managing `organizations`, which are core context entities.  \n      Organizations are the highest ancestor in the parent hierarchy.  \n      They define access boundaries and are often the minimal primary scope for permission and resource management.',
    count: 5,
  },
  {
    name: 'requests',
    description:
      'Endpoints for handling incoming *requests* such as contact form submissions, newsletter signups, and waitlist entries.',
    count: 3,
  },
  {
    name: 'entities',
    description:
      'Endpoints that operate across multiple *entity types*, such as `users` and `organizations`.\n      *Entities* are identifiable domain objects that may be contextual, hierarchical (with parent/child relations), or actor-like.\n      These endpoints offer shared logic across modules, including slug validation and entity visibility.',
    count: 1,
  },
  {
    name: 'system',
    description:
      '*System level* endpoints for administrative actions and platform wide functionality.\n      These endpoints support operations such as user invitations, file uploads, and webhook handling.',
    count: 4,
  },
  {
    name: 'auth',
    description:
      '*Authentication* endpoints supporting multiple sign-in methods, including email/password, OAuth (Google, Microsoft, GitHub), and passkeys (WebAuthn).  \n      These routes cover user sign-up, sign-in, password recovery, email verification, account linking, and impersonation for system admins.',
    count: 26,
  },
  {
    name: 'attachments',
    description:
      'Endpoints for managing file based *attachments* (e.g. images, PDFs, documents) linked to entities such as organizations or users.\n      Files are uploaded directly by the client; the API handles metadata registration, linking, access, and preview utilities.',
    count: 5,
  },
  {
    name: 'metrics',
    description:
      'Endpoints for retrieving system level statistics and basic observability data.\n      Includes internal metrics as well as simple, high level counts for entities such as `users` and `organizations`.',
    count: 2,
  },
  {
    name: 'pages',
    count: 6,
  },
];
