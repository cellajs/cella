import enAbout from '../../../locales/en/about.json';
import enApp from '../../../locales/en/app.json';
import enCommon from '../../../locales/en/common.json';

const locales = {
  en: { about: enAbout, app: enApp, common: enCommon },
};

export default locales;
