/**
 * List all files containing default mutations to ensure
 * QueryClientProvider can handle offline mutations correctly.
 *
 * Add your app mutation files here to include them in the provider.
 */
import '~/modules/attachments/query-mutations';
import '~/modules/memberships/query-mutations';
