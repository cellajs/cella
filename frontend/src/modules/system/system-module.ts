import { registerModule } from 'shared/module-registry';

registerModule({
  name: 'system',
  owner: 'cella',
  scope: 'frontend',
  description: 'System administration UI for platform-wide settings.',
});
