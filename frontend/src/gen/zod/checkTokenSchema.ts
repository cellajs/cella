/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { apiErrorSchema } from './apiErrorSchema.ts'
import { z } from 'zod'

export const checkTokenPathParamsSchema = z.object({
  id: z.string(),
})

export const checkTokenQueryParamsSchema = z.object({
  type: z.enum(['email_verification', 'password_reset', 'invitation']),
})

/**
 * @description Token is valid
 */
export const checkToken200Schema = z.object({
  email: z.string().email(),
  role: z.union([z.enum(['member', 'admin']), z.null(), z.null()]),
  userId: z.string().optional(),
  organizationName: z.string().optional(),
  organizationSlug: z.string().optional(),
  organizationId: z.string().optional(),
})

/**
 * @description Bad request: problem processing request.
 */
export const checkToken400Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(400).optional(),
    }),
  )

/**
 * @description Unauthorized: authentication required.
 */
export const checkToken401Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(401).optional(),
    }),
  )

/**
 * @description Forbidden: insufficient permissions.
 */
export const checkToken403Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(403).optional(),
    }),
  )

/**
 * @description Not found: resource does not exist.
 */
export const checkToken404Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(404).optional(),
    }),
  )

/**
 * @description Rate limit: too many requests.
 */
export const checkToken429Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(429).optional(),
    }),
  )

export const checkTokenMutationResponseSchema = z.lazy(() => checkToken200Schema)