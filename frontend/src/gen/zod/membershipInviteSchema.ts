/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { apiErrorSchema } from './apiErrorSchema.ts'
import { z } from 'zod'

export const membershipInvitePathParamsSchema = z.object({
  orgIdOrSlug: z.union([z.string(), z.string()]),
})

export const membershipInviteQueryParamsSchema = z.object({
  idOrSlug: z.union([z.string(), z.string()]),
  entityType: z.enum(['organization']),
})

/**
 * @description Number of sended invitations
 */
export const membershipInvite200Schema = z.number()

/**
 * @description Bad request: problem processing request.
 */
export const membershipInvite400Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(400).optional(),
    }),
  )

/**
 * @description Unauthorized: authentication required.
 */
export const membershipInvite401Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(401).optional(),
    }),
  )

/**
 * @description Forbidden: insufficient permissions.
 */
export const membershipInvite403Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(403).optional(),
    }),
  )

/**
 * @description Not found: resource does not exist.
 */
export const membershipInvite404Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(404).optional(),
    }),
  )

/**
 * @description Rate limit: too many requests.
 */
export const membershipInvite429Schema = z
  .lazy(() => apiErrorSchema)
  .and(
    z.object({
      status: z.literal(429).optional(),
    }),
  )

export const membershipInviteMutationRequestSchema = z.object({
  emails: z.array(z.string().min(4).max(100)).min(1).max(50),
  role: z.enum(['member', 'admin']),
})

export const membershipInviteMutationResponseSchema = z.lazy(() => membershipInvite200Schema)