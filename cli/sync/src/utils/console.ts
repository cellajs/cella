/**
 * Console Utilities
 *
 * Shared console output helpers for consistent CLI logging.
 */

import pc from 'picocolors';

/** Green checkmark prefix for success messages */
export const checkMark = pc.bold(pc.greenBright('✔'));
