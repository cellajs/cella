{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "..",
    "noEmit": false,
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false,
    // Node-only VM agent: no DOM globals (the infra config adds "dom" for the
    // Pulumi program's fetch typing; Node's own types cover fetch here).
    "lib": ["es2024"],
    "types": ["node"]
  },
  "include": [
    "src/**/*.ts",
    "../lib/args.ts",
    "../lib/env-file.ts",
    "../lib/errors.ts",
    "../lib/escape-regexp.ts",
    "../lib/fetch-like.ts",
    "../lib/guards.ts",
    "../lib/is-main.ts",
    "../lib/json.ts",
    "../lib/retry.ts"
  ],
  "exclude": ["src/**/*.test.ts"]
}